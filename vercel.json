{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install && npm run typecheck",
  "buildCommand": null,
  "outputDirectory": null,
  "rewrites": [{ "source": "/mcp", "destination": "/api/mcp" }],
  "functions": {
    "api/mcp.ts": {
      "maxDuration": 60
    }
  }
}
